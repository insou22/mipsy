use crate::error::RSpimResult;
use super::context::Context;
use crate::inst::instruction::InstSet;

pub fn generate_text(_context: &mut Context, _iset: &InstSet) -> RSpimResult<()> {
    
    
    Ok(())
}