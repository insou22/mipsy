use crate::{
    MipsyResult,
    MPProgram,
};

pub enum Warning {

}

pub fn check_program(_program: &MPProgram) -> MipsyResult<Vec<Warning>> {
    let warnings = vec![];

    // TODO

    Ok(warnings)
}