use crate::{
    RSpimResult,
    MPProgram,
};

pub enum Warning {

}

pub fn check_program(_program: &MPProgram) -> RSpimResult<Vec<Warning>> {
    let warnings = vec![];

    // TODO

    Ok(warnings)
}